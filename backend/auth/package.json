{
  "scripts": {
    "start": "node index.js",
    "build": "tsc",
    "update": "npm run dockerbuild & npm run dockertag & npm run dockerpush",
    "dockerbuild": "docker build -t ocaks.azurecr.io/k8s/backend-auth:latest .",
    "dockerpush": "docker push ocaks.azurecr.io/k8s/backend-auth:latest"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.22",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
